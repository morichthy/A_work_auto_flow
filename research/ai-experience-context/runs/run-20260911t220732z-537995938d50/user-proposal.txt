首先，根据问题，建立搜索数据的简化问题和关键词。
A：
先按默认分词或向量匹配命中结果，默认取来源为概览与经验，然后返回展开后的内容（按研究过程展开）。
AI判断返回的结果是否和当前问题相关，如果相关则依据当前的内容进一步返回（完整文稿）。
根据返回的文稿，AI判断要取多少的内容作为问题起始的关键上下文内容进行之后的回答。

B：如果A失效，找不到相关条目，则改变来源为“研究经过”，然后重复A。还不行则改变来源为“技术内容”。

C：如果A和B还找不到相关内容，则减少或优化关键词或问题来扩大搜索范围。

D：如果后续解决问题的结果不佳，在A，B，C的基础上，除了返回展开后的内容，还需要返回关联补充的内容作为上下文，再尝试进行问题的回答。