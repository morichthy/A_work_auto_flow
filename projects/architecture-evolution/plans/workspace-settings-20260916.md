# 工作区设置与无子Agent兼容

2026-09-16；Owner PRJ-ARCHITECTURE-EVOLUTION；[Run](../runs/run-20260916t051529z-eec543c75781/run.json)。

目标：工作台可调整协作策略、默认召回数量、上下文和模型预算；配置由程序读取并缓存，不要求AI每次打开配置文件。继承[上一阶段重排结果](../runs/run-20260916t035620z-93aca3a94bae/CLOSURE.md)，不重做或改写其固定证据。

## 功能与清单

- [x] 先构造核心基线并运行红测：缺模块时失败，原日志`.local/workspace-settings-red.log`。
- [x] 唯一设置文件、严格契约、CAS原子保存、进程缓存和外部修改失效。
- [x] HTTP/CLI/工作台专页；新材料查询与AI阅读模板采用设置，已有RS和显式请求维持预算。
- [x] auto/off协作策略；host无子Agent时使用单Agent路径，查询后端不依赖子Agent。
- [x] 功能、缓存开销、组件/浏览器/构建和真实setup旧工作区升级恢复验证。
- [x] 现行文档/Skill、测试登记、Run证据、全文回读、refresh-index/validate。

## 状态与边界

唯一权威为根目录`workspace-settings.json`。缺文件使用内置默认，不创建文件；损坏拒绝。服务端使用stat签名缓存已验证值，保存必须比较revision并持互斥锁。工作台恢复默认只改变草稿，保存后才生效。设置是新请求默认值，不能替代访问控制或覆盖已固定预算。

协作开关是面向遵守本工作区工作流的AI的策略；外部宿主是否有子Agent工具仍由宿主决定，不能声称项目代码可以禁用任意宿主工具。任务入口读取精简程序输出，后续检索由代码取默认值。旧search/CTX兼容入口的参数暂不迁移，明确说明作用范围。

尚未修改本工作区实际偏好；默认auto保持既有行为。实现与验证使用隔离临时配置。无新依赖、无新模型；前端分发包含预构建资源，用户无需Node。

## 已验证与本轮变化处置

设置13项、阅读41项、前端48项、浏览器2项及真实setup18项通过；配置warm读取p95约0.7553毫秒、2000次无重复JSON解析。详细方法、失败、原始日志和限制见[完整结果](../runs/run-20260916t051529z-eec543c75781/RESULTS.md)。源码/记录登记、完整文稿回读和结构校验已完成；[收尾回执](../runs/run-20260916t051529z-eec543c75781/CLOSURE.md)保存固定身份、索引及范围。

本阶段完整成果已同步（范围：统一设置，报告MEM-afa98edf-0a9e-57cd-aba6-56d9c3681152 r1；AI一致性自查）。FTS/vector均generation34；validate为0错误、21项既有警告。未发布，人工及第二物理机验收另列。

| 基线/变化 | 阅读范围与处置 |
|---|---|
| 前轮重排概览MEM-3227e150… r1及报告MEM-5c827f1d… r1 | 公共接口固定回读，SHA与前轮一致，完整阅读，impact无变化；限定前轮重排实验，保留当时默认值历史 |
| 本轮新设置技术结果与Run | 没有同范围既有文稿，新增独立技术单元和完整报告；不能把设置缓存数字当检索加速 |
| 其他16项历史技术内容 | 前轮已判定属于旧设计/实现；本轮设置不改变其固定正文，不宣称覆盖整个Project历史 |
| 现行文档与Skill | 六文档5改1核对无需改；补设置手册，8项活跃Skill及发现入口全部核对，work-loop/material-query更新，其余保持 |
