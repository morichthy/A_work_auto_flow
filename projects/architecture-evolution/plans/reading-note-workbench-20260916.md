# 阅读笔记上下文与工作台入口修复

归属：PRJ-ARCHITECTURE-EVOLUTION。验证 Run：RUN-20260916T155857Z-A3D7B61BDD32。

## 本次目标

- 解释并明确 JSON 会话状态与人/AI 可读 Markdown 的职责；在 context/reading-notes/ 提供有版本标记的阅读副本，不建立第二个权威来源。
- 修复已有 RS 在工作台不可见、列表刷新清空正文及未默认显示当前笔记的问题。
- 首页突出当前笔记，观察环境操作改为紧凑工具栏；证据入口默认关联当前笔记的真实来源。
- 材料导航按对象/记录主题展示，点击进入对应系统记忆；对象与会话标题分清楚。
- 保留权限、来源变更、固定指纹、预算、用户内容和已有工作台状态修复。

## 已知根因与边界

- 旧 ReadingSessions.list 清空正文、只列会话，不自动读取。
- 旧列表先对随机 UUID 目录分页，再按 Owner/授权过滤，可能出现空首页。
- 目标 RS 以限定 Owner 授权域创建，普通本机工作台的更广授权域与其不相等；修复必须保留原 RS 实际查询范围，不能借本机查看扩权。
- HEAD.json 的 notes 是内部状态；AI 通过接口返回的 context_markdown 使用笔记，不直接装载整个 HEAD。
- 本次不重新执行浮点实验，不改变既有科学结论；历史测量与证据保持原字节。

## 验收清单

- [x] 列表、授权超集/不足、旧会话、Markdown 副本与失败边界回归：31项通过。
- [x] 当前笔记自动展示、选择/刷新、跨对象隔离、首页入口、主题跳转组件与真实浏览器验证：17组件通过；浏览器5/6通过，既有规模N0超时单列未修。
- [x] 前端类型检查、构建与60源码/65资源指纹一致。
- [x] 私人 Markdown 不进入源码发行；19项Windows分发/升级用例最终通过，包含真实setup扩展旧工作区预览/升级/重复升级/恢复。
- [x] 原真实 RS r5合法回读并生成可打开 Markdown；1 note/2未记候选partial保留。
- [x] 六份现行文档及受影响 Skill 按影响检查，测试登记audit通过。
- [x] 固定Run输入/结果已登记，refresh-index成功，validate为0错误21既有警告；规范文稿回读收口单独见本Run的memory-closure/CLOSURE.md。

## 实施记录

2026-09-16：开始修复。策略为 auto；GPT-6 低推理子 Agent 分担后端、阅读组件和首页入口，主 Agent 负责范围/接口协调、分发保护、文档与验收整合。保留工作树已有未提交改动。

2026-09-17：功能和目标链路验证完成；最终构建指纹f3cb77c7455a4360318329c08658c98ef512b4b95dcf94dd55f6fa9380561b8b。完整浏览器规模用例仍失败，因此验证Run保留failed，不声称全库回归通过。目录逐会话重验仍约20秒/5会话；后续性能优化不混作本次已完成。

