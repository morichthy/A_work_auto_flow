# 阅读笔记的对外名称

用户要求：对外显示问题/会话名称，避免长RS-ID成为主要名称。

本次局部修改沿用RS已有goal作为可读名称，不新增另一份命名状态，也不冒充已绑定聊天宿主的任务标题。工作台列表原已显示goal；补充有名称的导出文件名与折叠技术身份，context/current.md以问题名称为一级标题。长目标仅在标题/文件名适度缩短，完整目标和原RS身份保留。目录不改名，旧链接与并行问题隔离保持。

验证：扩展现有快照测试，检查旧展示格式按需刷新而不改RS；前端组件/类型/真实下载与构建；按缓存显示版本影响检查Windows旧工作区升级恢复。实际结果完成后追加。不改上一Run已固定的源码与回执。

## 完成结果

context最近3份笔记已按原权限检查重新生成名称标题，RS修订/语义更新时间保持不变。显示格式v3使其他旧展示副本在下次选中时更新；旧路径不迁移。

后端相关10项通过（54.217秒）；前端19项组件、类型检查、1项真实浏览器下载通过，资源及指纹更新为index-BIIjfctt.js，见[前端结果](reading-note-names-20260917-ui.md)。最终真实setup.cmd扩展旧工作区升级/重复升级/恢复用例通过（112.673秒）。测试目录审计837项、无未分类或缺失；refresh-index完成，validate为0错误、21项既有警告。没有运行无关全量回归，也没有新发布或改历史科学依据。

日志保留于本工作区`.local/reading-note-names-{backend,upgrade,validate,refresh}.log`；本次为局部名称显示跟进，不覆盖上一Run固定证据。已核对受影响AI_READING说明与现有Skill入口，无新增语义状态或外部聊天标题绑定。
