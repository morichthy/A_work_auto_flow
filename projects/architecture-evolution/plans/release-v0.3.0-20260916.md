# v0.3.0 正式发布清单

Owner：PRJ-ARCHITECTURE-EVOLUTION。用户已明确授权正式提交及发布当前版本，无需再次询问。

## 范围与依据

沿用 origin 对应的 https://github.com/livky/A_work_auto_flow ，在现有 v0.2.0 后发布 v0.3.0：增加双语查询规划/用户术语库、升级保护和成果整理 Skill，附完整机制说明。只提交审查过的框架文件，本地业务记录、来源登记、私人词库及运行数据保留本地，不放入发行附件。依赖锁与模型未变，核验并复用上一版依赖包。

已读 context/START_HERE、NOW、work-loop、development-checks、DEPENDENCY_RELEASE、TESTING、UPGRADE_TESTING、DEVELOPMENT_HISTORY 和前次发布清单。六份现行文档及全部活跃/兼容 Skill 由独立只读审查核对；主代理整合结果。完整后端回归独立执行，主代理负责实际发行包、依赖及远端验证。

## 工作步骤

1. 核对未提交框架差异与发布边界，完成六文档/Skill 审查。
2. 完整后端、安装升级/恢复和必要前端验证；旧 B01 失败及物理机/质量验收独立报告。
3. 正式提交并推送框架；从对应提交 git archive，检查文件边界、前端指纹和必要入口。
4. 核验匹配依赖 ZIP 及逐文件清单，验证实际源码离线安装。
5. 创建草稿 Release，上传源码 ZIP、依赖 ZIP 及各自 SHA256；核验远端内容、大小与摘要后公开。
6. 保存最终提交、标签、附件哈希、验证日志及发布回执，更新当前状态。

验证输出目录：`.local/release-20260916/`。当前状态：已正式提交、推送并公开发布；下面保留分阶段经过，最终结果见末节。

## 发布前审查与构建

- 六份文档前后完整核对；README、ARCHITECTURE、CORE、文档索引、维护约定和TESTING与实现一致。ARCHITECTURE核对日期更新，重复gitattributes规则清除。
- 8项活跃Skill的适用时机、公开动作、发现/安装入口逐项一致；无兼容方法目录，保留2个退休入口的恢复保护。
- query_plan/reading的scope、固定版本、原RS累计预算、分页计划冻结和未引用内容维护边界经独立只读审查；实际行为仍以回归和既有AI场景证据为准。
- 暂存34个框架文件，不暂存私人业务数据。预览树5793ce658bc5cdc10079870fffba96ee874a23de，实际git archive为519文件，必要入口、业务排除、前端source/assets指纹通过。
- 复用依赖包SHA256 `7810752e15ff40b88e04bae0eedae57f20355ca2da51febba7505a787361d3a4`，4085文件逐项哈希、当前锁及模型清单匹配。
- 新建本地验证Run：RUN-20260915T211906Z-DC3A7C82C601。最终日志、指纹与远端回执收口后固定登记，进行中的日志不提前登记为不可变产物。

## 提交与已完成验证

正式提交 `89ad30753589fb7b919b3db75cdf91d47449f4ca` 已推送并回查origin/main一致。正式源码SHA256 `e5081b4d76dbdd18a1908f27550acf6bd920ac8761f8015ad4cf7ed61b90ebdc`，519份文件与预览树实际测试字节完全一致。

实际预览源码ZIP四步离线验收通过：首次安装158.625秒、升级108.875秒、接收端再打包43.672秒、依赖恢复44.547秒；扩展旧工作区220个文件与222个目录全部保护一致。新旧包清单身份相同。范围是同一台Windows物理机上的隔离目录，不代表第二物理机/真实业务验收。

前端38项组件、typecheck及16项普通浏览器场景通过。执行代理误将原暂缓的万节点/十万关系场景纳入首次E2E，60秒超时，原日志和失败上下文保留，不计通过；不必要的普通场景重复复验已中止（当时6/16），不作为结果依据。现有构建source/assets指纹匹配，无前端产品变化，因此没有重建资源。

此前双语查询及成果整理的实际AI合成自查所固定19项相关源码/方法/测试/模板指纹与当前字节一致；沿用原证据及其局限，未将本次静态核验当作新AI行为测试。GitHub草稿389490364的四个附件已全部上传并逐项核验远端大小/SHA256。完整后端回归仍进行中，公开前须收口实际回执。

## 发布完成

2026-09-16，v0.3.0已公开：https://github.com/livky/A_work_auto_flow/releases/tag/v0.3.0 。标签、origin/main与89ad307一致，公开后再次回查四个正式下载链接、大小和SHA256。

完整后端610/610具备逐ID终态，609通过、1项既有B01失败、0 error/skip；B01独立复验仍为冻结fixture清单不匹配。最后14项关系测试默认沙箱因tmp目录WinError5导致tempfile重试，栈诊断和单次mkdir证实，以获批隔离测试权限重跑14项全部通过（15.194秒），未改产品/ACL。所有中断、调用错误、失败与最终结果保留并去重，不将不完整回执计作通过。

固定结果载体为[发布验证Run](../runs/run-20260915t211906z-dc3a7c82c601/RESULTS.md)，包含逐文件源码清单、完整测试日志及远端publication回执。Run保留failed以表达B01失败，发布状态独立为完成；不声称全绿、科学复核、第二物理机或规模验收通过。

本阶段完整成果已同步（范围：v0.3.0框架发布及验证记录；版本：89ad307与Run内publication回执；AI一致性自查）。当前NOW与Project入口已同步，业务数据仍保留本地。

## 同步到第二个GitHub账号（2026-09-16）

用户要求同时推送/发布到另一账号，并明确确认正确拼写为`morichthy`。已创建公开仓库`https://github.com/morichthy/A_work_auto_flow`，保留origin并增加morichthy远端；main已推送并回查为同一提交89ad307。复用原v0.3.0的四个已核验附件与发布说明，保留B01及规模/验收限制；代码和包字节无变化，不重复软件测试。

同步发布已完成：https://github.com/morichthy/A_work_auto_flow/releases/tag/v0.3.0 。公开后回查标签与main均为89ad307，四个附件的大小/SHA256与原Release一致；正式下载链接已核验。向原发布Run追加独立MORICHTHY_PUBLICATION.json和MIRROR_RESULTS.md，不改写此前固定回执。
