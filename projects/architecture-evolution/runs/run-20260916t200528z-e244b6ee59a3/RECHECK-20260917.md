# 同分支修改后的两种阅读模式复验

日期：2026-09-17。用户要求确认当前工作区固化的联想与快速模式是否仍正常。本次只读复核产品并运行隔离测试，没有修改源码、生产配置或既有阅读会话；原 RESULTS 与固定证据包保持不变。

## 结论与依据

当前未发现联想或快速模式被同分支修改覆盖、破坏。以本 Run 的 `.run-captures/final-evidence/manifest.json` 为基线，检查开始和测试结束各核对一次：499 个受控源码文件全部存在且 SHA-256 一致。这里比较的是上次最终交付快照，而不是 Git HEAD；不据此声称用户没有修改其他业务材料或此前已纳入快照的内容。

当前运行的隔离回归：

| 检查 | 结果 | 回执 |
|---|---|---|
| 三模式策略与真实存储边界 | 13 项通过，57.560 秒 | recheck-20260917-modes.log |
| 默认配置、兼容、保存冲突及实际 HTTP | 19 项通过，1.115 秒 | recheck-20260917-settings.log |
| 设置、当前阅读与阅读记录组件 | 26 项通过 | recheck-20260917-ui.log |
| TypeScript 类型与预构建来源/产物指纹 | 通过，499 个受控文件 | recheck-20260917-ui.log |

## 固化机制核对

- 联想：已有 note 的缺口/线索转为实际子问题，保存 association_text 与 clue_sources；启用开关、轮次上限、查询条件及固定来源校验仍在。同 RS 保留原目标、授权、预算与历史；跨 Owner 综合 note 依据或底稿变化后不再静默交付旧综合稿。
- 快速：实际交付片段逐条 assess，只有已接受且指纹有效的来源能用于 quick note；保留 block 定位和未阅读全文限制。拒绝判断、伪造片段引用、切回标准绕过全文门槛均有测试。quick note 后仍能继续读取同 Owner 后续召回片段。
- 调用链：公开接口、直接 reader 派工说明、material-query Skill、成果整理 Skill 与工作台的模式控件一致。`.agents` 发现入口继续指向现行 workflow，未发现旧方法副本接管。
- 工作台：默认模式保存与当前会话修改分开；并发保存冲突保留草稿，历史 legacy 会话不显示不受支持的编辑器。保存配置本身不会启动后台 AI。

快速模式通过 reader 流程与 note 来源校验实现“仅用召回片段”；它不是全局禁止用户通过公开来源入口手动查看正文的访问控制。当前未发现工作区根 `workspace-settings.json`，因此新会话使用程序默认 standard、允许联想、最多 3 轮；工作台仍可显式保存自己的默认配置，本次未替用户写入。

本次没有重跑真实浏览器、实际 AI 三流程或安装升级；对应受控源码与既有验证快照一致，因此沿用原 Run 的证据及限制。没有验证正在运行的旧工作台进程是否已重启，也不据软件通过断言真实业务的笔记质量、召回收益或科学结论成立。
