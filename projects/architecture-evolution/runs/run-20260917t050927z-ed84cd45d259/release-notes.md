本版完善记录独立上下文、材料阅读和证据导航，并交付匹配的 Windows x64 离线环境。

## 主要变化

- 所有对象的 L0–L4 记录须独立交代背景、条件、内容、结果和边界；L2 明确研究经过，L3 给出可复用做法。更新相关 Skill，并要求生成后逐条独立回读。
- “证据与影响”显示逐结论确认状态、复核主体与范围，区分已保存复核和当前依据有效性；折叠重复结构字段，保留作者引用编号及固定版本链接。
- 对象记忆主类型收敛为 L0–L4 与研究文稿；必要的目标、问题、检查点和复核等工作流类型保留在辅助入口。
- 增加标准、联想、快速阅读模式和设置；完善最近问题笔记、正文、公式、图片及证据详情导航。
- 增加可选离线 Cross-encoder 正文重排，保留固定候选、预算、撤权和失败回退边界；支持子Agent偏好设置与兼容旧工作区升级。

## 下载与升级

下载 `framework-source-v0.4.0.zip`、`dependencies-windows-x64.zip` 及各自 `.sha256`。完整解压源码，将依赖 ZIP 和校验文件放到 `setup.cmd` 旁边。

首次安装：`setup.cmd --register --open`

升级：关闭旧工作区的工作台、索引和实验进程，在新版源码目录执行：

```powershell
.\setup.cmd --target "D:\研发\旧工作区" --register --open
```

升级后继续使用旧目录。新旧目录不能互相嵌套；已有数据、来源、用户规则及自定义 Skill 保留。源码包含预构建前端，使用端无需 Node 或逐包安装 Python 依赖。

## 兼容范围与验证

Windows 10/11 x64 CPU，Python 3.12.10，标准384维嵌入模型；配套包含标准可选Cross-encoder。自定义模型名称、路径、维度或GPU环境不属于自动迁移保证。

发行源码提交：`4c20ea22d2c15c66615395879f9714f4572de295`。实际源码ZIP为568个文件，私有实例排除和前端source/assets指纹通过；源码SHA-256为 `dd94ad1a829990e0463d2bd38ea026f72c8bf4463a6700feaa441b60f6115c8e`。

配套依赖包4092个文件逐项SHA-256、锁和模型清单一致，包SHA-256为 `90216697586c48921d3f729c983ed74cfae4b1f60d310a0af0e6dc804819fc82`。实际源码离线安装、扩展旧工作区升级、接收端再打包和依赖恢复通过，226个文件与227个目录全部保护一致。最终测试适配只改变浏览器脚本和source指纹，安装器、模型及前端运行资源字节未变；最终ZIP另经完整指纹和依赖预览验证。

前端85项组件与类型检查通过；24个常规浏览器场景经首轮和定向复验逐用例去重后全部有通过记录。首次Windows测试目录改名失败和旧UI测试选择器失败已保留，并已复验。

完整后端按实际discovery和用例ID去重覆盖745项：744通过、1项既有B01失败、无错误或跳过。B01为冻结测试材料清单不匹配：三份材料与清单都逐字节等于v0.3.0，独立复验仍失败，不能视为全绿。主进程后段遭遇沙箱临时目录创建WinError5，诊断后受控停止，以获准权限仅续跑未完成51项，全部通过；未改产品权限或历史fixture。

本机隔离软件验证与AI内容自查不代表第二台物理机、真实业务质量或科学结论确认。万节点规模场景仍暂缓；不承诺任意长文重排或一般检索效果。研究实例和本地验收材料不随公共源码附件分发。
