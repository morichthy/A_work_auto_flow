数值复核输出已产生但绘图失败：首次复核经过
L2 研究经过
浮点求和
数值稳定性
研究经过

已检查当前访问边界；未重新复核科学结论。

结论确认状态

未登记可独立复核的结论；记录已保存不代表结论已确认。

以上为已保存的复核记录；本页未重新核验支持证据的当前有效性。

研究经过
问题背景

浮点求和研究已有三轮固定输入：1000个单位增量与大数前后排列（3组）、三项 
[
10
16
,
1
,
−
10
16
]
[10
16
,1,−10
16
] 的全部排列（6组）、该三项重复10次后的30种排列。原结果显示逐项累加及部分Kahan输入有误差，Neumaier和 math.fsum 在所测后两轮均精确。为解释误差从哪一步产生，需要读取同一份保存数组，复算最终输出、记录中间状态，并生成比较图表。

本研究比较有限精度求和在“大数与小量共存，随后正负大数抵消”的输入上是否丢失小量。输入是保存脚本实际接收的有限 binary64（53位有效精度的双精度浮点）数，均无量纲；原复核环境为 CPython 3.12.10、Windows 11 x64。

参考值把每个实际浮点输入精确转换为有理数后相加。记输入为 
𝑥
𝑖
x
i
	​

（
𝑖
=
1
,
…
,
𝑛
i=1,…,n，
𝑛
n为项数）、算法返回值为 
𝑆
^
S
，则

𝑆
r
e
f
=
∑
𝑖
=
1
𝑛
Fraction
⁡
(
𝑥
𝑖
)
,
𝑒
a
b
s
=
∣
Fraction
⁡
(
𝑆
^
)
−
𝑆
r
e
f
∣
.
S
ref
	​

=∑
i=1
n
	​

Fraction(x
i
	​

),e
abs
	​

=
	​

Fraction(
S
)−S
ref
	​

	​

.

Fraction表示精确有理数转换；“精确命中”指绝对误差 
𝑒
a
b
s
=
0
e
abs
	​

=0，不使用容差。这只检查已表示浮点数的求和误差，不混入十进制文本转换误差。

实际经过与观察

首次执行逐步复核与绘图任务的运行编号为 RUN-20260908T214955Z-2273015D2CEF。命令退出码为1，已生成数值输出，但Matplotlib绘图库未能完整导入，整个任务未成功结束。原执行回执和标准错误日志分别记录失败状态及环境异常，不能只凭目录里已有数值JSON就宣布整次复核通过。（原运行及日志见固定依据）

当时选择保留这次失败的运行与已产生文件，修复绘图依赖可读性后另开运行复试；不覆盖原日志，也不把依赖导入错误写成算法数值反例。

本条结论与复用条件

本条记录的是执行环境失败：完整复核和绘图尚未完成，而不是已证明原三轮数值错误。重试前应确保绘图依赖可读，并按SHA-256（文件内容指纹）核对固定输入，成功重试必须保留独立运行身份。

时间与边界

原记录时间：2026-09-08T21:54:18.825929Z。本条于2026-09-17根据既有材料补写，未重新执行实验。证据只覆盖所列构造输入与已保存实现；非整数、极端指数、溢出/下溢、NaN/无穷值、跨平台及运行时间尚未验证。数值复算通过与科学确认是两件事：本次未提交确认，仍无人工科学认可。

固定依据

原始运行与附件保留以下固定来源；它们用于追查本条明确描述的复核内容，不代替正文背景。

1（run.json）。

2（全文）。

3（全文）。

方法定义：浮点求和方法，4 r1。

参考文献

[1] 三轮求和的逐步误差轨迹与细节复核

[2] execution.json

[3] stderr.txt

[4] 浮点求和方法：精确基准、递推与适用条件

[5] 首次逐步复核命令退出1；数值输出已产生，Matplotlib环境无法完整导入。

结构化记录字段
关键上下文
记录层级

L2

保存原因

使本条正文脱离对话及相邻记录仍能理解问题、依据、结果和使用边界。

对象概览
未提供
引用与依据
归属 浮点求和的数值稳定性：顺序、补偿与抵消
使用输入 三轮求和的逐步误差轨迹与细节复核
使用输入 execution.json
使用输入 stderr.txt
引用 浮点求和方法：精确基准、递推与适用条件
引用 首次逐步复核命令退出1；数值输出已产生，Matplotlib环境无法完整导入。
下游影响
引用 抵消求和如何验证：精确基准、失败轨迹与算法选择边界
引用 浮点求和数值稳定性：整体概览
引用 后续与证据入口
引用 复核与证据边界
引用 数值复核输出已产生但绘图失败：首次复核经过
固定版本与技术信息