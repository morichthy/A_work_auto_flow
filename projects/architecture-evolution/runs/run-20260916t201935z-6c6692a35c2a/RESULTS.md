# 阅读笔记叙事深度与存储职责

## 用户要求与实际改动

用户质疑note为何必须放.local/用JSON，并要求其包含既定要素且是一篇足够详细、可读、可续接研究的简化完整文稿。存储位置并非AI/外部工具要求，而是此前会话实现选择：reading.py BASE指定.local/reading-sessions，reading_snapshot.py生成context/reading-notes/<RS-ID>/current.md。JSON保存查询/来源/进度/预算/请求/历史及正文，Markdown为可读投影；JSON易于程序处理这些字段，但原子写入、并发检查、恢复并非JSON格式专属，Markdown frontmatter加适当机制也可实现。

单一Markdown可以保存一篇当前文稿；若替代整个会话状态，需要明确如何处理版本/固定引用/授权和进度，不能直接把既有JSON删除。本轮范围是解释存储选择与改进写作，因此不移动/删除RS或改变JSONschema。现有字符串/数组项可写多段Markdown，每字符串上限12000字符，渲染仅双换行拼接，不强制短条目。

本轮仅在直接worker_brief、material-query及AI_READING/CORE等说明强化叙述：按实际依据讲清问题起点、假设/方法选择、实验/推导、失败与认识修正、当前结论边界及下一步；标题为覆盖要求，不是每栏一句。公式、变量/单位、初值和更新顺序、条件、数字及引用嵌入相应论述；推断与材料明确记载区分，quick不得超实际片段编造完整历史。预算是容量上限，不是越短越好的目标；去重后仍超限明确缺口，不静默删研究逻辑。没有新增主Agent固定复检。

## 软件与写作验收

现有delegation10项通过（46.985秒），Owner相关7项通过（0.010秒）。没有新增仅匹配文案的测试；未改渲染/schema/存储/前端/预算/依赖或安装扫描，因此不重做全库/前端构建/安装测试。后续补“递推初值/更新顺序”仅文字，无逻辑变化。并行三策略功能变更保留，不据这些局部测试宣称其全范围通过。

独立terra低推理reader实际读了此前固定完整research_process MEM-ab9db095-2545-594c-9e94-f72bab43c464 r1，形成example/narrative-note.json首稿。开发检查发现Kahan初值缺失及解释性连接需标示，reader另存v2补齐，并调内部标题层级；不能声称首稿自动完美。没有改写旧证据/旧笔记。

NARRATIVE_NOTE.md由既有note_markdown渲染v2，核心叙事1961字符，整note4876字符，当前UTF-8字节代理估算8538；旧6000容纳不下，本次14000样例窗口足够。保留11个固定引用，normalize_note_sources与正文ID校验针对旧已交付回执通过。这不是当前来源授权的再次证明。所有图、原始数组、Run和轨迹本次未展开/复算；pairwise缺口仍保留。样例是固定历史文稿的可读改写，不是新的科学结论。

## 实时会话尝试与未解决项

为了展示实际context副本，另建standard样例RS，只有本次note上限14000，未改全局默认；已知主题检索重排设off，不是排序/延迟评测。reading-recall失败，公开回执为INTERNAL“材料完整性检查失败”；保存了原始请求/回执/计时，未继续read/note，未宣称当前RS笔记已替换。

只读诊断：当前浮点Owner27条规范记录、来源闭包和局部完整组装全部通过。公开错误适配隐藏了底层MemoryError细节，旧回执不足以定位触发记录；无证据认定磁盘损坏或并行策略导致。未重跑长召回、修索引、改权限或原件。失败测试RS通过公开archive归档，保留历史；这项故障仍需单独定位。

此次交付为写作要求改进与可读固定文稿样例，真实新RS端到端验收未通过。源码未发布；没有迁移存储、修改全局预算或把样例文件当权威会话输入。

最终refresh-index成功；validate通过（0错误、21既有警告）。

登记阶段另有一次owners.list_owners读取业务元数据时的JSONDecodeError（Invalid escape，line1/col785），首试登记未写入。紧接单独resolve_owner再次成功；没有捕获到错误文件路径，不能把这个间歇观察与此前召回失败直接等同或断言其原因。下方登记重试使用同一固定产物。
