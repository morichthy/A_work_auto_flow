# v0.3.0 — 双语查询与完整成果维护

## 主要更新

- 阅读会话支持有界双语查询计划、保护型号/数值、领域术语库及独立召回路线；沿用原范围、固定版本和累计预算。
- 新增“整理更新当前成果”Skill，重要结论、阶段交接和实质变化时阅读当前概览与全文、盘点新旧记录及未引用变化，维护章节和文稿根并做一致性检查。日常有用中间记录可自主保存。
- 完整解释 record/revision/commit、manifest、最新检索、概览与完整文稿的关系，并从CORE对应章节链接。
- 用户术语库与发行默认模板分离，升级仅补缺并保留用户字节，恢复支持回到原不存在状态。

## 安装与升级

Windows x64 CPU。下载本页的源码ZIP、独立依赖ZIP及各自SHA256；解压源码，将依赖ZIP和校验文件放到setup.cmd旁。

首次安装：`setup.cmd --register --open`

升级：关闭旧工作区的工作台、索引和实验进程，从独立新版目录执行 `setup.cmd --target "D:\研发\旧工作区" --register --open`。继续使用旧工作区；新旧目录不得嵌套。已有数据、配置及自定义规则保留，冲突规则按安装回执合并。

不需要使用者逐个安装Python包、Node或Docker。标准模型兼容边界仍为约定名称/路径和384维；没有宣称任意模型可自动迁移。

## 版本和附件

源码提交：`89ad30753589fb7b919b3db75cdf91d47449f4ca`。

源码SHA256：`e5081b4d76dbdd18a1908f27550acf6bd920ac8761f8015ad4cf7ed61b90ebdc`。

依赖锁和模型未变化，复用已核验依赖包，SHA256：`7810752e15ff40b88e04bae0eedae57f20355ca2da51febba7505a787361d3a4`；包ID：`d53325b59c213a1a229afd0caa42ee4c3c300779592fc0e09b5f31170e4987e4`。

实际源码附件为519文件；业务实例、私人词库、来源登记、运行数据与数据库已排除。4085个依赖文件逐项哈希和源码前端指纹通过核验。

## 验证与限制

- 后端完整覆盖610/610：609通过，1项既有B01失败，0 error/skip；B01独立复验仍为历史冻结夹具清单不匹配，未改写历史材料来消除失败。不是全绿回归。
- 前端38项组件、TypeScript检查及16项普通浏览器场景通过。误触发的原暂缓万节点/十万关系场景60秒超时，保留失败记录；重复普通场景复验中止，不重复计数。
- 实际源码ZIP完成离线首次安装、扩展旧工作区升级、接收端再打包及依赖恢复；220个受保护文件、222个目录全部保持。正式源码与受测预览的519份文件逐字节一致。
- 六份核心文档及8个活跃Skill前后核对；测试登记audit通过。双语查询和成果整理的既有实际AI合成自查沿用已核对一致的源码/Skill指纹，原局限保留。
- 工作区校验0错误、19个既有历史快照链接警告。末段测试曾因沙箱tmp目录WinError5陷入Python临时目录重试；单次mkdir及调用栈定位后，以获批隔离测试权限完成14项（15.194秒），未修改产品或ACL。

软件通过、实际AI自查、人工/科学复核分别表达；历史检索质量、万条规模和第二物理机验收不因本次发布自动通过。全部部署验收限同一台Windows物理机上的隔离合成工作区。
