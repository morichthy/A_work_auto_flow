# 开发决策与演进记录

D39（2026-09-17，知识笔记与类型详情）：用户明确 note 应保存对搜索有帮助的材料知识，不承载 reader 派工状态。沿用 RS 状态契约，在渲染边界分离 question/decision 与知识正文；材料演进、条件、反例和“可选的下一步建议”保留，联想问题及关键词由主 Agent 制定。不是只隐藏界面文字，直接派工与 Skill 同步约束。工作台集中首页阅读选择/模式控制，记忆摘要跳统一证据详情；文稿沿固定结构展开，图片按登记授权及指纹展示，结构引用不当作科学文献。反复 Owner 警告源于 tools/registry.json 数组成员被误按顶层身份解析，修适配器并隔离无关目录诊断。真实浮点 L1 r3 缺正文来源位置，仅 figure:0 有确定映射，故规范新写作而不凭空改历史。实施与先红后绿验证见 RUN-20260917T021448Z-BC1AAF379A2C；未发布，最终通过范围以 Run 结果为准。

D38（2026-09-17，最近笔记与证据详情）：当前入口以recent列出最近24小时更新的问题，历史不删除；snapshot按选中RS快速展示。原列表逐RS强核验且首页预载全库证据，使已存MD也需长时间等待，改为小型授权元数据/HEAD失效检查和按需详情，正式handoff继续强核验。编号引用保留FixedRef深链；界面链接排版与AI交接预算分开，避免装饰使旧note超限。证据采用标题/结论/ID搜索及单条详情，不用新增图数据库或后台AI。最终93项阅读、10项新增、44组件、5浏览器及19项Windows升级通过；现场热快照约0.2秒，不代表强核验提速。实际笔记经公开API修订r9公式格式；早期CLI完整性失败与后续API成功差异未定位，失败保留。证据见本工作区RUN-20260916T212839Z-337776B112FC，未发布。

D37（2026-09-17，note叙事与存储职责）：用户指出笔记应是可读、细节充分的简化完整文稿，讲清问题发展、方法/实验和认识变化，而非各标题下的精炼条目。现有字符串/数组项已支持多段Markdown，渲染仅拼接，不需要改schema或存储；直接派工与Skill加强叙事、关键细节及预算不是压缩目标的要求，quick不得编造全文过程。明确.local/JSON是此前会话状态管理的工程选择，不是正文必须用JSON；单MD可以承载当前文稿，若迁移需另解决会话元数据与历史真源，此轮不擅自迁移。固定旧浮点文稿的叙述样例与局部验证见RUN-20260916T201935Z-6C6692A35C2A，不追加主Agent固定审核。

D36（2026-09-17，三模式Owner阅读）：用户要求将现有Owner全文阅读明确分为standard、associative和quick。选择保持RS的`mode=owner_document`，另冻结strategy、association与association_text，避免把存储身份、阅读策略和联想方向混成一个状态。standard与associative都在相关性判断后读取Owner完整文稿；associative仅在RS信息不足时根据实际搜索文本追加召回，受enabled/max_rounds约束。quick逐条判断实际交付片段，只以已接受片段写同质量note，明确不覆盖全文。跨Owner理解通过synthesize固定Owner note底稿和实际来源；任一底稿/来源改变即不再交接旧综合稿。联想线索保存依据、条件、未知和下一步，不自动晋升关联或结论。configure采用CAS且只保存模式/方向，不启动后台AI。软件测试、浏览器和真实独立AI验收分别记录，不能由本条设计说明替代。

D35（2026-09-17，reader默认自查与引用漏登）：实际note首稿遗漏递推公式及Run/图引用，原程序只能证明sources有效，不能证明正文引用全部登记。用户要求固化reader提取、自查与可确定检查，明确不增加主Agent固定审核。直接worker_brief与material-query要求同一reader建立相关内容/来源对应、写完自查补齐后保存；reading-note本地核对证据正文中的显式来源ID是否已交付并列入固定sources，拒绝时不写笔记。不新增字段、模型/搜索调用、审核Agent或交接清单；不强制包含所有已交付引用，不回溯审核旧note。公式重要性及推理完整性仍属AI语义职责，字符串检查不能担保。选择局部保存边界而非额外主侧审核以减少上下文/延迟；实际验证见note-quality-defaults-20260917计划及RUN-20260916T195725Z-16CDCD28570D。

D34（2026-09-17，Owner文稿阅读与note预算）：用户指出21次内部搜索的重复excerpt/matched_text和排名诊断不应占据主Agent上下文，且逐候选短note遗漏技术经验。新模板显式owner_document，旧RS缺mode维持legacy；复用召回/重排但在AI交付边界按固定正文块去重，只交正文与owner_id，每页每Owner先交一段、未确认时渐进续页，选中后跳过该Owner其他片段。逐Owner读取最新完整过程文稿，报告/真实正文回退明确标记，图片及L0/Run保留固定引用并按需展开；不把文稿不可读说成不存在。研究式note保存问题/条件、认识、推理/假设、关键细节/出处、限制与下一步；出处允许已交付ID简写，程序补齐固定引用并拒绝未知/歧义，避免手抄指纹错误。内部诊断不扣最终note预算，设置统一提供Owner阅读数和最终note保守token估算；工程资源保护逐操作，旧累计账本保留。选此方案是减少AI冗余交互及预算混淆，不采用宿主特定自动压缩/Agent SDK或全库Run自动总结；代价是需维护Owner文稿、判定note语义覆盖，估算不能代表宿主精确token。只有原生run.json无MEM正文的Run仍不直接进入三层召回；已有Run总结引用支持固定读取。软件、实际AI及范围负例分别记录于本工作区owner-document-reading-20260917计划和RUN-20260916T181657Z-4D7EFFF321BA，不宣称整体检索质量或等负载延迟提升。

D33（2026-09-17，阅读上下文与工作台入口）：用户实际找不到浮点阅读笔记，并要求首页直接展示可读上下文、关联证据及可跳转主题导航。继续以 RS JSON 为唯一会话状态，增加 `context/reading-notes/<RS-ID>/current.md` 派生阅读副本；不用可编辑 Markdown 替代版本/预算/授权账本，避免双重真源和额外 AI 调用。修复列表先分页后过滤及更广本机授权域不能查看限定 RS 的问题，同时保留原会话范围。首页与阅读记录共享选择，默认打开有笔记的会话；“当前对象”是 Owner，“阅读问题”是 RS。材料导航使用公开记忆对象/记录接口，不再拿目录中的 md 文件名冒充主题。相关记录仅表示来源导航，不能自动认定所有 L0–L4 都支持笔记。实现与实际验证见本工作区 reading-note-workbench-20260916 计划和 RUN-20260916T155857Z-A3D7B61BDD32；旧设计和失败证据保留。

D32（2026-09-16，工作台监测与状态）：Run登记的发布ZIP被周期扫描并经证据校验间接哈希，导致超限/等待；观察路径改为Owner记录与HEAD范围，并向证据核验注入可选有界哈希器，正式核验默认不变。单文件诊断允许页面展示其他节点，但失败快照不覆盖监测基线。前端采用标签页内首次挂载后保留，隐藏冻结输入并暂停关系轮询；证据启动预载，系统记忆默认研究经过、对象记忆显式生成。比持久化整份页面缓存少一份数据真源，代价是刷新浏览器不恢复临时结果，已访问页面占用内存；查询TTL、授权与对象切换隔离仍有效。文稿类型选择仅改变下次读取参数，成功换文才替换当前文章并清派生上下文。验证与限制见RUN-20260916T154043Z-CFB59D215DFF及workbench-state-fixes-20260916计划；未发布。

D31（2026-09-16，子Agent能力偏好）：在现有协作配置添加自由文本subagent_requirements，由工作台编辑，程序缓存并原文传给宿主选模型；默认较低能力、低成本和低推理。相比固定厂商模型下拉框，这样允许不同宿主表达可用模型与能力，但不保证模型存在或自动执行。off和权限约束优先；不增加AI配置解析调用。schema1旧文件只读补默认、原字节不变，新写请求保持完整严格，避免旧客户端覆盖新偏好。内置默认随源码发行，私人设置不入公共包且升级保留。验证以RUN-20260916T081757Z-4C8B6EB90E82为准。

D30（2026-09-16，子Agent阅读）：用户要求由低成本独立Agent承担长材料查询交互，主Agent只使用reading note。采用宿主无关reading-delegate任务包与reading-handoff有界笔记，实际派工在宿主Skill完成，沿用现有auto/off配置和RS唯一状态；无宿主支持时同RS单Agent回退。相比内置某一厂商Agent SDK/后台任务库，这样避免新增模型凭据、运行依赖和重复状态；代价是宿主必须实际提供并调用子Agent工具，检索程序不能计量外部AI token。交接再次核验授权和固定版本，整笔记装包，过长/陈旧/覆盖不全公开缺口；当前阅读正确性仍需AI/人判断。实现、真实独立AI与边界测试以本工作区reading-delegation-20260916计划及RUN-20260916T060608Z-6199BA92B86E为准。

D29（2026-09-16，工作区设置）：用户要求兼容无子Agent宿主、可调召回数量和上下文预算。采用独立schema1用户文件，由程序严格读取并以stat签名缓存；工作台专页和CLI共享CAS，不让AI每次解析文件，不引入后台配置服务。新模板/页面消费默认值，旧请求与RS维持已固定预算；协作auto/off是工作流策略，宿主能力仍外置。相较合并进检索模型配置，这样避免把AI协作控制与模型/来源授权耦合。代价是独立CLI启动仍有首次读文件，手动外部编辑需要通过完整校验，不能强制无视规则的外部AI。旧search/CTX保持兼容参数，未宣称整体检索延迟提升。工作区计划：projects/architecture-evolution/plans/workspace-settings-20260916.md。

D28（2026-09-16，正文重排实施）：用户要求先功能清单、核心基线，再开发RRF候选池→必要正文→Cross-encoder+条件→最终排序。冻结小型中英合成候选后，新增READ窄提供器和显式字段条件模块，复用固定回读/RS/Ledger；旧RS无字段保持off，新模板auto。选择离线多语言ONNX分类模型并复用现有依赖，不用生成式答案评分，不改变召回向量。未知条件不等于冲突，候选不因低分删除，超长/不完整本窗回退。代价是更多回源与推理以及模型组件分发；开发集完整上下文改善Recall但nDCG不处处优于短输入，条件自然语料全unknown时不伪造收益。软件回归/真实模型/离线交付结果与限制以本次Run为准，不覆盖旧独立评测失败。工作区计划：projects/architecture-evolution/plans/retrieval-ranking-latency-20260916.md。

本页回答“为什么这样开发、从哪些错误中改过来、哪些讨论尚未定案”。**全局架构/工作流方案前完整阅读，局部方案按相关决定定位；开发后维护实际结果。** 当前功能和模块分别以 [CORE](CORE.md)、[ARCHITECTURE](../ARCHITECTURE.md) 为准，本页保存选择理由，不复制接口手册或完整聊天记录。

整理日期：2026-09-10。下列旧阶段依据已有文档回溯整理，时间按原文声明；不是本日重新执行或重新确认了历史结论。

## 仍影响开发的选择与来由

D27（2026-09-16，成果整理Skill）：用户认可保留 document→章节→技术块结构，要求重要成果记录时完整维护全文和上下文。新增 consolidate-results，由 work-loop 在明确记录重要成果、阶段交接或实质变化时调用；有用中间记录仍可自主提交。选择完整读取概览/文稿、盘点文稿固定引用至当前的变化及未引用内容、修订必要记录与文稿根、全文回读；不采用单一大文稿覆盖所有技术记录，也不新增后台语义代理或同步数据库。代价是阶段收口需要实际阅读和显式维护，固定旧引用须解释，缺依据/未处理差异不得声明已同步；程序保存、文稿同步和科学复核独立。本轮保留已有工作树和历史固定内容，验证及限制见工作区计划 `projects/architecture-evolution/plans/consolidate-results-skill-20260916.md`。

D25（2026-09-15，用户确认后实施）：针对中英材料召回不佳，reading-recall接入AI填写的少量等义计划、保护文本、领域词库、独立词法/dense和单跳低权重关联召回。保留原问题、规范身份路和固定块，按排名融合并保存计划/来源；技术/文献问题或可靠英文来源需英文补查，由material-query指导AI判断。选择复用RS和既有模型、将同义/关联分开，避免新后台翻译服务或把相关概念当成等义。用户词库与公共种子分离，升级只补缺、回滚恢复。代价是多路编码/阅读及诊断输出消耗原预算，候选条件仍须AI核对。23项阅读、16项基础召回与18项真实升级检查通过；实际双语离线向量4次编码/128 token，目标技术单元首位，但仍有不相关候选和partial过滤提示，不能宣称业务检索质量提升。真实AI导出时还发现空value错误回执的CLI解引用问题，修复与针对性验证保存在本轮Run。详细依据：本工作区`projects/architecture-evolution/plans/work-context-bilingual-query-20260915.md`和`RUN-20260914T174350Z-B0D5AFF7D7C3`；旧D19/D20初检扩词选择与小样本限制保留。

| ID / 阶段 | 当时的问题或讨论 | 选择、理由及当前代价 | 状态与详细依据 |
|---|---|---|---|
| D01 / 2026-09-08 起：对象与版本 | 已有 Research、Run 等身份，另建平行对象会让来源和修订难以对应；保存成功也不能代表索引或结论已经有效 | 复用业务 Owner、不可变修订和固定引用，分开保存/索引/复核。保留旧版读取，但不承诺跨 Owner 原子事务 | 沿用；[首版设计](design/system-memory/01-data-contracts.md)、[当前保存与回执](MEMORY_USAGE.md) |
| D02 / 2026-09-09：分层与文稿 | 单靠事件、经验和地图不足以保留完整技术分析；按层堆卡片也难以形成连贯报告 | 增加完整技术单元，继而把检索说明、技术块、独立章节和双文稿分开。旧 map.report 是中间方案，兼容读取，不回写旧字节。代价是技术正文与文稿组织需要分别维护 | 已演进到 v3；[分层演进](design/system-memory/L0-L4-REFACTOR.md)、[中间编排](design/system-memory/RESEARCH-REPORT-COMPOSITION.md)、[技术文稿设计](design/RESEARCH_DOCUMENTS.md) |
| D03 / 2026-09-09 至 10：材料查询 | 表示规则、某篇材料实际可读内容和临时查询结果混在一起，难以说明查了什么、缺什么以及如何深入 | 增加独立材料查询应用，复用原规范存储；分开类型、实际表示与材料包，保留固定范围、预算与回源。先接词法/身份和既有关系；代价是新旧检索入口与受限适配并存，模型策略不能由接口名称推定 | 首期已接入，体验仍有反馈；[原核心讨论](design/representation-query-v0.2/baseline-v0.1/CORE.md)、[继承与限制](design/representation-query-v0.2/RUNTIME_STATUS.md) |
| D04 / 部署演进：旧数据保护 | 只测简单目录，无法暴露用户增加多层业务目录、目录工具等情况下的升级错误 | 用真实 setup 的扩展旧工作区 fixture 核对文件哈希、空目录、重复升级与恢复；源码和依赖分开交付。代价是触及这些边界时验证较重，普通文案修改无需重做整套迁移 | 沿用；[故障与升级测试约定](UPGRADE_TESTING.md)、[依赖分发](DEPENDENCY_RELEASE.md) |
| D05 / 2026-09-10：现行文档体系 | 旧核心说明、历史阶段计划和当前架构混用，Project 影响图与根说明重复维护；开发入口越来越重 | 六份短文档维护现行用途、架构、功能、索引、文档规则和测试；详细手册按需，旧稿原地分类保留。本页补充演进理由，避免精简入口时把来由一起丢掉 | 文档规则已采用；[文档索引](README.md)、[维护约定](DOCUMENTATION_MAINTENANCE.md)；本条依据本轮用户反馈 |
| D06 / 2026-09-10：验证强度 | 每次开发固定执行全套命令，可能让小改动承担无关测试；现有 quick 也不是最小定向集合 | 按重要性、改动大小和失败后果选轻量核心、详细关键功能或 full；AI 场景按影响增加。现有工具不改，必要边界测试仍保留 | 文档规则已采用，非测试器重构；[测试规范](TESTING.md)、[工具真实边界](TESTING_REFERENCE.md) |

## 测试基线维护补充（2026-09-10）

D07：用户要求明确测试点如何产生和持续维护，并同意补齐基线防线依据。采用现有 catalog 逐项增加 `baseline` 风险、保留理由和复审触发条件，TESTING 维护准入、复审与退出标准，避免再建重复清单。本次已补录现有 24 项，revision 48；登记审计及 32 组前后选择比较通过，成员与原执行字段不变。静态补录不追认历史选入原因，也不表示重新运行基线或完成全部新能力的风险覆盖审查。当前没有自动检查依据完整性或自动调整基线的功能，代价是相关变更和发布仍需开发审查。

## 历史错误与尚未闭合的讨论

| 观察或反馈 | 对后续方案的提醒 | 当前状态 / 依据 |
|---|---|---|
| 软件功能回归与检索留出题集质量结果不一致 | 能运行不等于找得准；检索方案需要独立质量、延迟和上下文成本证据 | 历史质量未通过、万条规模暂缓；[原状态台账](design/system-memory/STATUS.md) |
| 材料已保存，但来源回读或索引覆盖可能缺失 | 分别核对登记、固定原件、索引和复核；重新建索引不能补回原件，也不能消除历史指纹差异 | 来源位置兼容已有修复，历史缺口按原审计保留；[来源维护](EXISTING_MATERIALS.md) |
| 人工反馈：不能反选、结束原因不清、完成任务仍显示待计算 | 后续需验证真实交互和用户理解，不以底层调用成功关闭界面问题 | 已修复并完成相关组件/浏览器软件验证，仍待用户复验；本工作区人工检查 H02/H03/H16 |
| 用户希望摘要查询限定来源层级，当前主要是表示组合；层级与文稿定位也有新的讨论 | 明确内容职责、查询范围及文稿关系；不要把界面改名或更换层号当成内容已经整理 | 目标已按 D09/D10 实施，旧业务语义整理仍须实际审查；本工作区人工反馈改进计划 v3 |

完整工作区中的详细讨论、用户原文和执行证据，从 `projects/architecture-evolution/README.md`、`docs/DOCUMENT_HISTORY.md`（该 Project 内）以及 `plans/human-acceptance-improvements-20260910.md`（该 Project 内）追溯。公共源码不包含这些项目实例；上表是可分发的框架决策摘要，不声称附带全部历史验收证据。

## 分层目标澄清（2026-09-10）

D08（已被 D09 更正）：讨论中曾将论文式完整正文归入 L0。用户随后明确这是口误：L0 元数据/原始材料追溯不变，L1 完整技术论文块不变，仅修改 L2–L4。保留这段讨论来由，不再沿用“重定义 L0”的方案。

D09（已实施，新内容 v4）：用户同意 L2 研究经过、L3 条目化经验与认识、L4 项目/研究整体概览。L2 保存有依据的路线和转折，操作日志留在 Run；L3 区分观察/结论、假设与建议并保留条件和证据；L4 包含当前阶段、限制和未解决问题。层级不是可信等级，也不要求逐层复制。查询采用“概览与经验”，默认查 L4/L3，需要时展开 L2，再展开 L1；不能只换名称而继续混入细碎正文。新 narrative/overview 与分类 experience 使用 v4；独立文稿 level=null，L0/L1 不变。旧 event/map 保留原语义和固定版本，另写新内容须实际整理，不自动调整层号。

D10（已实现并完成定向软件验证）：主查询从“表示转换”改为内容来源选择：概览与经验查 L4/L3、研究经过查 L2、技术内容查 L1，读取已有正文。展开依明确固定引用，可从经验直接到技术依据；来源选择不等于访问上限，显式排除与原预算仍约束展开。保留旧表示接口兼容。缺内容、缺关联、权限/版本变化和预算不足继续如实报告，不能通过隐藏真实缺口伪装成功。

实施教训：浏览器真实组包发现旧经验的原生 Run claim 虽已登记，却不能经新 Reader 定位。修复为索引定位已知 Owner、读取对应 Run 并逐项核验固定指纹与登记来源，保留撤权/范围拒绝；不构建全库证据图，也不提升结论状态。新增故障回归。新旧契约、来源过滤与展开、界面实操和真实 setup 扩展旧目录已作验证；历史冻结 B01 指纹失败仍单列，不修改冻结材料追求通过。实际 AI/人工/第二物理机分别记录。

## 当前层级收敛与数据迁移（2026-09-11）

D11（已实施并完成浮点研究实际迁移）：用户发现工作台同时显示新旧 L2/L4，浮点研究的新类型内容为空。原因是此前只接通新类型与查询，没有完成既有内容迁移，且遗漏了普通界面入口和部分证据/续接调用方。当前选择是每层只有一个入口，已有内容经实际审查后以同一 ID 的完整新修订迁移；统一写服务只放行 event→narrative、map→overview，并要求前版固定来源、正文与变更原因。历史修订不改写，当前索引替换旧类型。相比创建新 ID 并留下两个当前版本，这样能保持身份和明确的迁移轨迹；代价是必须逐项补正文、展开关系并复核相关调用方，不能通过改名或仅重建索引完成。

实际结果：浮点研究的5条旧事件与1条地图在同一 ID 下生成完整新修订，1条经验补齐关系；7份前版逐项回读一致，FTS/向量均重建到 generation14。真实浏览器确认只有单一 L2/L4 入口且正文可读。前端33项、内容7项、维护20项及新增公开迁移场景、真实升级18项通过；memory 188项中187通过，历史冻结 B01 失败保留。实际查询的默认10秒累计预算不足，显式60秒完成三级读取与组包，未放宽默认值或宣称性能改善。

## 怎样继续维护

D26（2026-09-13，发布准备）：用户明确删除无用Skill并发布纯框架Release。删除research-loop/workspace-context兼容方法与三个旧空目录，旧名不再安装；已知旧发现文件仍按指纹可恢复退休，用户改写保留。补发行排除本地来源登记及当前任务状态，setup只补缺失的空初始文件，以免干净源码无法启动；不修改旧工作区数据。发布验证和实际远端结果另记，准备不等于已发布。 发行实测另发现生成契约CRLF导致git archive源码指纹不符，已改字节生成/检查并重建；完整依赖复制后的即时目录切换出现WinError5，延时同路径切换成功，增加有限重试且持续失败仍恢复，不放宽指纹或恢复冲突检查。

D25（2026-09-13，D24遗漏补全）：用户指出“沿用Owner”缺少比较标准，补查缺相关性与经验依赖判断，RS无法承担整个任务上下文。现明确比较目标、概览、成果与未解问题；同目标继续则沿用，主题相似仅引用。持续任务复用计划或一份可打开的Markdown清单，由AI维护当前输入/召回材料、理解、必要细节、尝试和下一步，RS继续负责阅读，不增加数据库或后台同步。补查由AI结合缺口、相关性和经验依赖判断，既可读深固定依据，也可扩大获准召回或先实验。代价是清单需AI主动更新、续接核验，不能宣称程序自动保证更新。前轮固定结果保留，本轮实际处置见工作区work-context-followup-20260913计划。

D24（2026-09-13，D23按授权实施）：统一短work-loop负责归属、记录和续接，material-query独立，默认7项Skill及2个非默认兼容方法。各Owner默认允许L0–L4、normal、auto_summary=false，按内容保存，不改写已有显式策略。补RS的Owner/固定检查点绑定、有界list、最新view和可逆归档；工作台展示真实阅读笔记、固定原文及版本导出。取消D22按扩查次数必问人的门槛，保留已ask_user须真实意见及原范围/累计预算。方法选择留给AI，程序仍守固定版本、权限、预算和单一写入边界；没有新增后台AI、数据库、模型或依赖。

整合教训：旧浏览器测试仍使用退役map/event按钮，升级样例误用循环残留Owner导致越界绑定，已按现行类型和正确对象修正，未放松权限或改旧记录。独立AI实际公开调用确认save_checkpoint可用；工具经验未召回源于必要Run不在调用者硬上限内，不能归因于工具不支持索引，也不能靠重置旧RS解决。setup仅对已知旧发现文件精确指纹做可恢复更新/退休，保留用户修改及附属文件，并提示合并不同规则。本轮598项全量首轮5处失败中，4处来自旧测试预期（含两个Owner子案例），修正后定向复验通过；既有B01冻结基线问题保留。38项组件、16项普通浏览器、18项升级检查及独立AI合成任务回执分别收口于工作区RUN-20260912T211413Z-6272D2E02162；历史质量、规模和第二物理机边界继续保留。

D23（2026-09-13，用户方向与待实施计划）：用户认为按研究/项目/算法问答/工具使用拆分入口过重，要求统一工作与记录逻辑，各类对象的原文、方法、用途、关联和使用结果均可分层成为可检索依据。计划采用短work-loop主入口，material-query独立；L0–L4按有用内容组合，不强制补层或每任务双文稿，文档步骤/他人经过不得冒充本次执行。沿用Owner与固定来源，工具源码、memory和runtime分离；run-execute仍为执行工具。计划同时处理重复规则、现行类型策略、Owner到RS的可见续接及第二次扩查过强人工门槛，保留权限/版本/验证边界。本轮未改Skill或程序，不视为已实施；细节见本工作区 `projects/architecture-evolution/plans/unified-workflow-skills-20260913.md`，D22的既有实现与历史选择保留。

D22（2026-09-13，按用户授权实施）：用户明确每次需要检索时多路跨层，短材料全部返回且带链接，长材料先命中块及条件，保留后完整阅读，再保存理解、连接和必要细节。采用现有 material-query Skill 与新增 reading CLI/HTTP；独立 RS 工作记录管理问题续接与累计预算，不增加规范知识类型、不另存原正文。第二次及以后扩查默认等待调用者记录真实人工意见，该字段不构成认证或扩大授权。代价是每层向量分别编码、跨进程续批需重建查询、来源复查有累计成本；程序只保证交付与定位，语义质量仍需AI/人判断。实现与验证见本工作区 `projects/architecture-evolution/plans/ai-reading-workflow-20260913.md`，原D17–D19的比较与被替代理由保留。

D21（2026-09-13，文档补充）：用户要求将记录、Run、三张索引表及AI字段生成的解释保存为CORE按需链接，保留直白概念、关键字段与实体文件，不扩充CORE正文。新增 [MEMORY_STORAGE_EXPLAINED](MEMORY_STORAGE_EXPLAINED.md)，在保存/检索功能下挂接，并将相关接口或功能开发时的主动维护纳入文档维护约定。强调L0统一视图与Run档案、记录与搜索条目、格式校验与语义复核的区别。此轮只维护说明，不更改运行逻辑或冻结实例；核对与验证见本工作区 `projects/architecture-evolution/plans/memory-storage-explained-20260913.md`。

D20结果补记：基础召回已完成最终81项定向和18项真实升级验证，当前26对象索引indexed、0规范写入；实际A07公开查询及完整组包通过AI自查。历史词法也已补正文块。四题跨语言首位正确，但每题Top-3仍含两条其他主题候选，不能宣称业务质量达标。早期能力标注/测试错误已修正；既存B01冻结指纹失败、万条规模和人工/第二机边界仍保留。下面的实施中记录保留原选择理由，固定详情见同一Run。

D20（2026-09-12，基础召回实施中）：用户将基础检索审计与补齐纳入本次开发，D18 暂缓的 L1 正文发现因此提前实施。已复现正文独有词查无结果，补充固定技术块的可重建投影；新 MQ 接入现有标准本地编码器的分组有界查询，并修正同一记录多个表示挤占候选窗口。选择复用 BM25/RRF 和既有384维模型，不增加额外稀疏/重排依赖；它们的收益尚无本地证据。技术单元标题提供确定性检索背景，AI补背景及初检扩词仍属于研究中的高层策略。真实模型四个跨语言合成题首位正确，但样本很小，不能覆盖真实业务相关性或万条规模；回归与迁移结果另行补记。细节和失败保存在本工作区 RUN-20260912T021858Z-B6E2DA1386D3，原D17–D19讨论理由保留。

D19（2026-09-12，研究讨论，未实施）：用户要求独立比较而非照着原 A/B/C/D 实施，明确同意从初检材料扩展查询，并要求保存候选、业界细节和最终选择理由。建立 RES-AI-EXPERIENCE-CONTEXT，由架构项目引用；v0.1 为方案草案。比较扩大到有界全文、技术块/多层独立召回及关系/全局综合；区分索引前补背景、命中后展开和接口返回正文。原方案保留对照，正文索引与参数收益待本地实验，不宣称新增 Skill 或最终选型。详细经过保存在该 Research，现行全局架构职责未改变。

D18（2026-09-12，用户澄清，尚未实施）：用户纠正 D17 的 A/B/D 理解：愿以更大阅读范围和时间换取底层细节召回，先展开再由 AI 判别；已有上下文能解决问题就不启动 D，解决不佳才增加关联深化。用户认可三部分上下文与覆盖清单。撤回“高层简短正文先筛除”作为默认建议，以及将独立反证搜索前置到 D 的误解；保留旧理由并在原方案标明替代。RAPTOR 跨层检索、混合召回/重排、Small2Big 与查询扩展提供可借鉴路线，但未定为最终实现。代码另确认 v3 L1 普通投影主要是检索说明、未含技术正文块，仅换来源/增大候选不能补齐此缺口；块级可重建索引列为候选改进。详细来源、修订流程与验证建议续写到 D17 的同一方案文件，未运行检索性能对照或创建新 Skill。

D17（2026-09-11，讨论建议，未实施）：用户提出以历史经验检索 Skill 作为研发区与 AI 的总入口，草案为高层先查、逐层兜底、改写关键词、失败后加关联。建议按问题覆盖缺口选择读取深度，必要条件/反证前置，完整文稿按需；OR 词法删词不保证扩大召回。总入口负责调度及同问题上下文保持，规范存储和查询仍走原服务。当前 MQ 无向量提供器，CLI 跨调用不保留查询，Owner 检查点也不等于独立问题会话；Skill 文字不保证宿主逐轮注入。先审议触发、上下文生命周期与薄客户端，后以固定问题集验证速度、关键遗漏和续接。详细建议留在本工作区 `projects/architecture-evolution/plans/ai-experience-context-skill-design-20260911.md`，未创建/安装新 Skill，未把建议改为运行要求。

D16（2026-09-11，已实施）：按用户七项要求增加标准类型多选、所有来源、5分钟默认活动预算、当前修订默认/显式历史、完整文稿去重、操作区与结果区整理，以及逐项 Skill 闭环。真实浮点研究暴露两个旧边界问题：直接对象范围被误用为必要依据上限，导致有正文却无候选；2 MiB累计读取预算不足以连续组装。现将直接范围与获准必要依据读取分开且保留显式排除，默认16 MiB/8万字符，仍逐次核验权限、版本并累计计量。没有用清零预算或放宽服务端授权解决。历史状态审计按固定依据回读，重新应用仍查当前 HEAD；新图表可选字段两端规范化，避免旧维护回执回填被误判篡改。完整文稿沿已有编排、逐块绑定图像，不增加第二套存储或自动写作。5项真实浏览器与18项真实 setup 升级/恢复通过；性能收益未作对照评估，不宣称提速。3个过时未注册方法原文固定后合并到 research-loop，8个工作区 Skill 逐项审查；用户自定义与全局插件不纳入清理。

D15（2026-09-11，架构约束补回）：用户要求继续查找精简中遗漏的开发指导。对照 Git HEAD 架构、当前规则与本轮 CORE 修改，发现对象不复制、查询历史非临时索引、影响提示不自动改业务结论等边界在架构入口弱化；补回 ARCHITECTURE 的稳定设计约束。根 AGENTS 的三项架构原则、现行模块影响表、状态表和实现限制原本仍在，本次补明确导航，不误称它们曾全部丢失。保留当前 v4 层级，不恢复已替代的 event/map 定义。教训是可压缩参数说明，但必须逐项保留稳定约束及其现行去向；文档维护与开发 Skill 已补此防线。本次仅恢复开发指导，不宣称完成依赖倒置或新增自动能力。

D14（2026-09-11，已纠正精简遗漏）：D13 精简时误删 G01–G12 功能定义与边界表，用户明确这些是开发必要内容。恢复功能块身份、输入输出、职责边界，补功能交接及到 ARCHITECTURE 模块影响表的映射；原 ARCHITECTURE 影响表一直保留。本次保留易读伪代码，不恢复字段级长说明。维护规则明确：精简实现细节不能删掉功能分工与影响导航。

D13（2026-09-11，已按反馈精简）：用户认为 D12 的字段、存储布局及算法参数过细。CORE 改用人类易读的伪代码、必要解释、选项差异和实现文件索引；AI 判断需要细节时再读代码。持续维护要求保留，但不再要求把参数级变化全部写入 CORE。D12 的核心机制记录目标继续沿用，详细展开方式由本条替代；本轮不改变运行行为。

D12（2026-09-11，已落实文档）：用户要求 CORE 准确解释关键算法、调用分支、关联/上下文结构，以及 Skill 如何指导 AI 解析结果、生成文本和提交本地存储。采用 CORE 集中维护机制与关键数据结构，保留运行契约的完整字段权威、ARCHITECTURE 的模块影响权威；不加入使用示例。发现 context-maintenance 仍指导新内容填写旧 event 字段，已改为当前 narrative/stages，不修改历史记录。本次不改变运行算法，也不把结构校验或索引成功等同于 AI 理解正确。核对与验证见本工作区 `projects/architecture-evolution/plans/core-implementation-documentation-20260911.md`；公共使用不依赖该项目实例。

讨论有实质结论或仍有关键分歧时，补记问题、选择/备选、原因、代价和待定项。开发完成后补实际结果、发现的错误、是否仍沿用以及依据链接；没有新的决策或教训时，在本次任务摘要注明已检查即可，不强行新增条目。

状态区分待讨论、已决定、已实施、已替代；决定不等于实现，实现不等于验收通过。旧选择被替代时保留其原理由，并链接新选择；不能改写为“从来没这样决定过”。历史日期或原因缺证据就标明未知，不反推或补造。

本页保持可完整读完的摘要：保留仍约束方案的选择、关键失败和未决问题；长论证、完整过程和旧阶段细节留在原设计、Project 或 Run，通过链接展开。已无当前影响的条目压缩成简短演进记录，不能无痕删除。
